<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nhập mã OTP</title>
    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url(../../img/login-page/computer-1867758_1920-min.jpg);
        }

        .otp-container {
            max-width: 400px;
            margin: 80px auto;
            background-color: #fff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.05);
        }

        .otp-title {
            font-weight: 600;
            margin-bottom: 10px;
        }

        .otp-input {
            letter-spacing: 6px;
            font-size: 1.5rem;
            text-align: center;
        }

        .resend-link {
            font-size: 0.9rem;
        }
        .custom-toast {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
            min-width: 250px;
            opacity: 1;
            transition: opacity 0.5s ease;
        }
    </style>
</head>

<body>
    {{#if message}}
        {{#if isSuccess}}
            <div id="toastMessage" class="alert alert-success alert-dismissible fade show custom-toast" role="alert">
                {{message}}
            </div>
        {{else}}
            <div id="toastMessage" class="alert alert-danger alert-dismissible fade show custom-toast" role="alert">
                {{message}}
            </div>
        {{/if}}
    {{/if}}
    <div class = "d-flex justify-content-center align-items-center vh-100">
        <div class="otp-container">
            <h4 class="text-center otp-title">Xác minh OTP</h4>
            <p class="text-center text-muted">Vui lòng nhập mã OTP đã gửi đến email của bạn</p>

            <form action="/forgot-password/verify-otp" method="POST">
                <div class="mb-3">
                    <input type="text" name="otp" class="form-control otp-input" maxlength="6" required placeholder="● ● ● ● ● ●">
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-primary">Xác minh</button>
                </div>
            </form>

            <p class="text-center mt-3 resend-link">
                Không nhận được mã? <a href="#">Gửi lại</a>
            </p>
        </div>
    </div>

    <script>
        window.addEventListener("DOMContentLoaded", function () {
            const toast = document.getElementById("toastMessage");
            if (toast) {
            setTimeout(() => {
                toast.style.opacity = "0";
                setTimeout(() => toast.remove(), 500); // remove after fade out
            }, 3000);
            }
        });
    </script>

</body>

</html>
